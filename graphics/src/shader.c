#include <neutron/graphics/shader.h>

NT_DEFINE_TYPE(NT, SHADER, NtShader, nt_shader, NT_TYPE_FLAG_DYNAMIC, NT_TYPE_NONE);

static void nt_shader_construct(NtTypeInstance* instance, NtTypeArgument* arguments) {}

static void nt_shader_destroy(NtTypeInstance* instance) {}
