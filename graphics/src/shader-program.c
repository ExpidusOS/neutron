#include <neutron/graphics/shader-program.h>

NT_DEFINE_TYPE(NT, SHADER_PROGRAM, NtShaderProgram, nt_shader_program, NT_TYPE_FLAG_DYNAMIC, NT_TYPE_NONE);

static void nt_shader_program_construct(NtTypeInstance* instance, NtTypeArgument* arguments) {}

static void nt_shader_program_destroy(NtTypeInstance* instance) {}
